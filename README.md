# -EventBoard-RTC-Driven-Message-Display-System
EventBoard is an RTC-driven message display system using the LPC2148 microcontroller. It displays scheduled messages on a 16x2 LCD based on real-time clock values. It supports admin editing, keypad input, LM35 temperature monitoring, LED status indication, and automatic real-time display.
